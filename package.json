{
  "name": "ierg4340",
  "private": true,
  "scripts": {
    "dev": "concurrently -n fe,be -c cyan,magenta \"npm run dev --prefix frontend\" \"npm run dev:backend\"",
    "dev:backend": "cd backend && set PYTHONPATH=. && python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8001",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@vnedyalk0v/react19-simple-maps": "^2.0.3"
  }
}
